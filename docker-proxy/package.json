{
  "name": "docker-proxy",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify"
  },
  "devDependencies": {
    "wrangler": "^4.4.0"
  }
}
